# Vite environment variables for .tsx pages (client.ts)
# This file is loaded by Vite from the frontend/ directory (Vite root).
# Use '/api' so requests go through the Vite proxy (same-origin, no CORS).
# In production (built files served from XAMPP), set to /hrdemo/api.
VITE_API_URL=/api
