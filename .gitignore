# ── Environment & Configuration ────────────────────────────────────────────
.env
.env.example
config.php
*.local.php

# ── Database ────────────────────────────────────────────────────────────────
*.sql
*.sql.gz
*.sql.zip
*.db
*.sqlite
muwasco (1).sql
# Schema migrations ARE version-controlled (schema-as-code)
!backend/database/migrations/*.sql

# ── Dependencies ───────────────────────────────────────────────────────────
vendor/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Build & Cache ───────────────────────────────────────────────────────────
/dist
frontend/dist/
/build
.cache
*.cache
*.log
temp/
tmp/

# ── IDE & Editor ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Uploads (user content) ──────────────────────────────────────────────────
uploads/*
!uploads/.gitkeep

# ── Diagnostic / scratch scripts (generated during development/probing) ──────
dump.js
probe*.php
probe_out.txt
facts*.txt
gather.ps1

# ── Runtime uploads (user content) ───────────────────────────────────────────
backend/app/public/uploads/
backend/storage/uploads/

# ── Archives & Backups ───────────────────────────────────────────────────────
*.zip
*.tar
*.gz
*.rar
hrdemo.zip
hrmuwasco.zip

# ── Composer ────────────────────────────────────────────────────────────────
# composer.lock IS committed: deterministic CI/production installs
# (regenerate deliberately via `composer update`; see Phase 5 report §10)

# ── PHPUnit ─────────────────────────────────────────────────────────────────
.phpunit.result.cache
phpunit.xml

# ── OS Files ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
# Runtime security counters (rate limiting buckets)
backend/storage/cache/rate-limits/
