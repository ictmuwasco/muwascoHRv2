<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ config('app.name', 'MUWASCO HR') }}</title>
    <style>body{font-family:system-ui,sans-serif;max-width:40rem;margin:4rem auto;padding:0 1rem;color:#1f2937}code{background:#f3f4f6;padding:.15rem .35rem;border-radius:.25rem}</style>
</head>
<body>
    <h1>{{ config('app.name', 'MUWASCO HR') }} — API backend</h1>
    <p>This Laravel application is the API backend for the MUWASCO HR system.
       The user interface is the React application in <code>frontend/</code>.</p>
    <p>API base: <code>/api/v1</code> &middot; Health probe: <code>/up</code></p>
</body>
</html>
